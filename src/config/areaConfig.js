/**
 * Default Area & Reporting Settings
 * ---------------------------------
 * Edit these values manually to change the default address
 * that appears in the application header and reports.
 *
 * These defaults are used when no saved settings exist in localStorage.
 */
const defaultAreaConfig = {
  district: "Uttar Dinajpur",
  block: "Raiganj",
  gp: "Sherpur",
  village: "Kokra",
  subcenter: "Khoksa SC",
};

export default defaultAreaConfig;
